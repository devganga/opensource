apiVersion: v1
kind: LimitRange
metadata:
  name: low-resource-limits
  namespace: nginx
spec:
  limits:
  - default:
      memory: "512Mi"
      cpu: "200m"
    defaultRequest:
      memory: "32Mi"
      cpu: "50m"
    type: Container